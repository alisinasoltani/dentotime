# Public browser requests should normally use the same-origin /api rewrite.
NEXT_PUBLIC_API_URL=

# Server-side rewrite target. HTTPS is mandatory for production builds.
BACKEND_INTERNAL_URL=http://127.0.0.1:8000
